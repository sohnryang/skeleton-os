/*
 * string.h -- string related functions
 */

#pragma once

#include <stddef.h>

size_t kstrlen(const char* s);  // Calculates the length of given string.
char* kitoa(int x);             // Converts int to string.
